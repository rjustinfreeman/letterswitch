---
import Twikoo from "~/components/astro/twikoo.astro"
import { common } from "~/config"
---

{
  common.comments && <common class="comments disabled" /> && (
      <common class="comments twikoo disabled" />
    ) && <Twikoo />
}
